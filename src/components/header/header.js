import "./header.scss";

const Header = () => {
    return (
        <header>
            <h1 className="fs-600 fw-bold header__title title">Your Adviser</h1>
        </header>
    );
};

export default Header;

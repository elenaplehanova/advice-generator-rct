import "./header.scss";

const Header = () => {
    return (
        <header>
            <div className="header__title title text-primary-900">Your Adviser</div>
        </header>
    );
};

export default Header;
